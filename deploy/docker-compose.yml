# Only used for building the agent image.
# borg-server runs directly on the host via systemd.
services:
  agent:
    build:
      context: ../container
      dockerfile: Dockerfile
    image: borg-agent
